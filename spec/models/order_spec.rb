require 'rails_helper'

describe Order, type: :model do
	before do
	end

	it "should not validate an order without a user_id" do
		#
	end

	it "should not validate an order without a product_id" do
		#
	end

	it "should not validate an order without a total" do
		#
	end
	
end